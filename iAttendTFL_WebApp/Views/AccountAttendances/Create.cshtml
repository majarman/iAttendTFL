@model iAttendTFL_WebApp.Models.account_attendance

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>account_attendance</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="account_id" class="control-label"></label>
                <select asp-for="account_id" class ="form-control" asp-items="ViewBag.account_id"></select>
            </div>
            <div class="form-group">
                <label asp-for="scan_event_id" class="control-label"></label>
                <select asp-for="scan_event_id" class ="form-control" asp-items="ViewBag.scan_event_id"></select>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="is_valid" /> @Html.DisplayNameFor(model => model.is_valid)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="attendance_time" class="control-label"></label>
                <input asp-for="attendance_time" class="form-control" />
                <span asp-validation-for="attendance_time" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
